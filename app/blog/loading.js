function Loading() {
    const cls = {
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    };
    return <div style={cls}>Loading...</div>;
}

export default Loading;
